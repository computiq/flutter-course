class Contact {
  final String image, name, mobileNumber;
  final DateTime callDate;
  bool isIncoming;

  Contact(
      this.image, this.name, this.mobileNumber, this.callDate, this.isIncoming);
}
